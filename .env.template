# Copy these values into:
# 1) Xcode -> Edit Scheme -> Run -> Environment Variables, or
# 2) mobile_project/Info.plist
#
# The app does not read this file directly on iOS. This file is the source-of-truth template.

AUTO_LEDGER_LLM_ENDPOINT="https://your-gateway.example.com/v1/ledger/recognize"
AUTO_LEDGER_LLM_API_KEY="your_api_key"
AUTO_LEDGER_LLM_MODEL="gpt-4.1-mini"
AUTO_LEDGER_LLM_SYSTEM_PROMPT="你是账单识别与结构化记账助手。只输出 JSON。顶层必须返回 {\"result\": {...}}。result 必须包含 amount, kind, category, paymentMethod, time, merchant, note, rawText, confidence, reason, recognizedEntryCount。amount 必须是数字或 null；kind 只能是 expense 或 income；confidence 必须是 0 到 1 的数字；recognizedEntryCount 必须是大于等于 1 的整数。优先提取实际支付金额/到账金额，不要把标价、优惠前金额、汇率换算金额、页面其他金额当作最终金额。优先交易时间，其次账单详情时间；若只能看到截图时间，需要在 reason 里说明并降低 confidence。若图片不是账单、或无法可靠识别 amount，则 amount=null，confidence<=0.35，并明确写出失败原因；不要编造金额。"
AUTO_LEDGER_LLM_USER_PROMPT_TEMPLATE="请根据截图和 OCR 文本提取一条最可信的账单记录。账本上下文：currencyCode={{currencyCode}}；localeIdentifier={{localeIdentifier}}；categoryCandidates={{categoryCandidates}}；paymentMethodCandidates={{paymentMethodCandidates}}；hasImage={{hasImage}}。OCR 文本：{{rawOCR}}。输出要求：1) 只返回 {\"result\": {...}} JSON；2) 若有多笔候选记录，返回最可信的一笔，并设置 recognizedEntryCount；3) 若 category 无法精确命中，返回最接近的中文类别；4) 若 paymentMethod 无法确定，返回“待确认”；5) 若 merchant 无法确定，返回空字符串；6) 若截图是教程页、安装页、快捷指令编辑页或其他非账单图，禁止编造金额。"
