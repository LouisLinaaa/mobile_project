# Copy these values into:
# 1) Xcode -> Edit Scheme -> Run -> Environment Variables, or
# 2) mobile_project/Info.plist
#
# The app does not read this file directly on iOS. This file is the source-of-truth template.

AUTO_LEDGER_OPENAI_ENDPOINT="https://api.openai.com/v1/chat/completions"
AUTO_LEDGER_OPENAI_API_KEY="your_api_key"
AUTO_LEDGER_OPENAI_MODEL="gpt-4.1-mini"
AUTO_LEDGER_OPENAI_ENABLE_THINKING="false"
AUTO_LEDGER_OPENAI_SYSTEM_PROMPT="你是账单识别与结构化记账助手。只输出 JSON 对象；不要输出思考过程或解释性文本。顶层必须包含 entries, recognizedEntryCount, primaryIndex。entries 为数组，数组元素必须包含 amount, kind, category, paymentMethod, time, merchant, note, rawText, confidence, reason。amount 必须是数字或 null；kind 只能是 expense 或 income；confidence 必须是 0 到 1 的数字。优先提取实际支付金额/到账金额，不要把标价、优惠前金额、汇率换算金额、页面其他金额当作最终金额。优先交易时间，其次账单详情时间；若只能看到截图时间，需要在 reason 里说明并降低 confidence。若图片不是账单、或无法可靠识别 amount，则该条 amount=null，confidence<=0.35，并明确写出失败原因；不要编造金额。"
AUTO_LEDGER_OPENAI_USER_PROMPT_TEMPLATE="请根据截图和 OCR 文本提取账单记录列表。注意 OCR 质量可能很差，请以截图为准。账本上下文：currencyCode={{currencyCode}}；localeIdentifier={{localeIdentifier}}；categoryCandidates={{categoryCandidates}}；paymentMethodCandidates={{paymentMethodCandidates}}；hasImage={{hasImage}}。OCR 文本：{{rawOCR}}。输出要求：1) 只返回 JSON 对象；2) 若有多笔候选记录，尽量返回所有可信条目，并设置 recognizedEntryCount；3) primaryIndex 指向最可信的一笔；4) 若 category 无法精确命中，返回最接近的中文类别；5) 若 paymentMethod 无法确定，返回“待确认”；6) 若 merchant 无法确定，返回空字符串；7) 若截图是教程页、安装页、快捷指令编辑页或其他非账单图，entries 为空数组。"
