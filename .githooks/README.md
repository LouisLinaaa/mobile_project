# .githooks

中文 / Chinese:
Git Hook 入口目录，由 `core.hooksPath` 指向。这里的脚本保持很薄，只负责转发到 `scripts/hooks/` 中的格式检查和构建检查实现。

English:
Entrypoint directory used by `core.hooksPath`. Scripts here should stay thin and delegate to the format-check and build-check implementations in `scripts/hooks/`.
