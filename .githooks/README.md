# .githooks

中文 / Chinese:
Git Hook 入口目录，由 `core.hooksPath` 指向。这里的脚本保持很薄，只负责转发到 `scripts/hooks/` 中的真实实现。

English:
Entrypoint directory used by `core.hooksPath`. Scripts here should stay thin and simply delegate to the real implementations in `scripts/hooks/`.
